{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "VITE_*",
    "PUBLIC_*",
    "ASTRO_*"
  ],
  "globalPassThroughEnv": [
    "AWS_ACCESS_KEY_ID",
    "AWS_SECRET_ACCESS_KEY",
    "AWS_SESSION_TOKEN",
    "AWS_REGION",
    "ALLOWED_ORIGINS",
    "ALLOW_EXTRA_SCHEMA_DATA",
    "BULK_MAX_ITEMS",
    "API_DOMAIN",
    "API_PORT",
    "AUTH_MIDDLEWARE_ENABLED",
    "AUTH_SECRET",
    "CREATE_TEST_OTP",
    "DATABASE_PORT",
    "DPG_SCORING_*",
    "FRONTEND_BASE_URL",
    "GOOGLE_GEOCODING_API_KEY",
    "INSTANCE_ENV",
    "INSTANCE_NAME",
    "INGEST_STREAM",
    "MATCH_SCORE_PROVIDER",
    "BEDROCK_*",
    "MATCH_SCORE_SERVICE_PORT",
    "CONSENT_CONFIG_*",
    "NETWORK_CONFIG_*",
    "NOTIFICATION_*",
    "PHOTON_URL",
    "POSTGRES_*",
    "REDIS_*",
    "SCHEMA_REGISTRY_URL",
    "SERVED_DOMAINS",
    "SIGNALS_PII_KEY",
    "SMS_TEMPLATE_ID"
  ],
  "tasks": {
    "build": {
      "inputs": [
        "$TURBO_DEFAULT$"
      ],
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "dist/**"
      ]
    },
    "build:tourist": {
      "inputs": [
        "$TURBO_DEFAULT$"
      ],
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "dist/tourist/**"
      ]
    },
    "dev": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "cache": false,
      "persistent": true
    },
    "dev:tourist": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "cache": false,
      "persistent": true
    },
    "preview": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "dependsOn": [
        "build"
      ],
      "cache": false
    },
    "start": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "dependsOn": [
        "build"
      ],
      "cache": false
    },
    "db:pull": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "cache": false
    },
    "db:push": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "cache": false
    },
    "db:generate": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "cache": false
    },
    "db:migrate": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "cache": false
    },
    "db:studio": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "cache": false
    }
  }
}