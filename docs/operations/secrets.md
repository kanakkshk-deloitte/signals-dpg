# Secrets

Three environments, three sources. The same set of keys is consumed per service
in each.

## Local development

- Plaintext `.env` at the repo root. It is loaded by
  `scripts/turbo-with-root-env.mjs` and surfaced to each workspace's process
  via Turbo, so every `pnpm dev:*` / `pnpm build` / `pnpm test` script that
  runs through root-level Turbo sees the same env.
- `.env` is gitignored (see `.gitignore`); `.env.example` is the canonical
  template. **Never put real values in `.env.example`.** If you change
  `.env.example`, change the corresponding `process.env.X` default in code so
  a fresh clone without `.env` still boots (see "Anti-patterns" below).
- `apps/api/drizzle.config.ts` does **not** go through Turbo — it reads
  `../../.env` directly via `dotenv.config({ path: '../../.env' })` so
  `pnpm db:push:api`, `pnpm db:generate:api`, and `pnpm db:studio:api` work
  without relying on the root env loader.
- The `apps/api/scripts/db_init.ts` helper (`pnpm db:init:api`) uses the same
  pattern.
- All env vars consumed by the API are validated at boot by Zod schemas in
  `packages/config/src/secrets.ts`. A missing required key crashes the
  process with a readable error before any request is served.

## Kubernetes — default values (`dpg/values.yaml`, charts repo)

> The Helm charts moved out of this monorepo into a separate deployment
> repository. The chart paths in the two Kubernetes sections below refer
> to that repo.

- The umbrella chart renders three Kubernetes Secrets from
  `templates/secrets.yaml`:
  - `dpg-postgres` — Postgres admin/user password (consumed by the bitnami
    Postgres subchart via `existingSecret`).
  - `dpg-redis` — Redis password (consumed by the bitnami Redis subchart).
  - `dpg-api-secrets` — `AUTH_SECRET`, `POSTGRES_USER`, `POSTGRES_DB`,
    `POSTGRES_PASSWORD`, `REDIS_PASSWORD` (consumed by the API pod via
    `envFrom: secretRef:`).
- Values are read from the `credentials.*` block at the top of
  `values.yaml`. The shipped file holds empty placeholders
  (`PG_PW`, `REDIS_PW`, `AUTH_SECRET` markers).
- `dpg/install.sh` fills those placeholders before `helm install`
  via its `generate_passwords()` helper, which writes:
  - `PG_PW = openssl rand -hex 16`
  - `REDIS_PW = openssl rand -hex 16`
  - `AUTH_SECRET = openssl rand -hex 32`
  An inverse helper resets the file back to placeholders before the
  next install run.
- `templates/secrets.yaml` skips emitting a Secret if its password is empty,
  so `helm template` / `helm lint` stay clean before `install.sh` runs.
- Notification-service secrets follow the same pattern — a
  `credentials.<chart>` block in `values.yaml` whose `keyId` / `secret`
  values must match the `internal-secrets.json` file mounted into that pod.
  (Match scoring now uses signals-search via a single `SIGNALS_SEARCH_API_KEY`
  — see the Match score table below — not an HMAC key pair.)

## Kubernetes — AWS overlay (`dpg/values-aws.yaml`, charts repo)

- **No External Secrets Operator, no AWS Secrets Manager wiring today.**
  `values-aws.yaml` only overlays ingress (ALB), persistence (EBS), and
  resource requests/limits on top of `values.yaml`. Secret material still
  comes from the same `credentials.*` block and is still rendered by
  `templates/secrets.yaml` at install time. The operator running
  `install.sh` is responsible for keeping the populated `values.yaml` out
  of git.
- If you need cloud-managed secrets here later (External Secrets Operator
  with `SecretStore` + `ExternalSecret`, AWS Secrets Manager, IRSA on the
  service account), it is **not** wired up today — that is a separate
  workstream and would require new templates plus an ESO install on the
  target cluster (cluster-wide, owned by the platform team, not by this
  repo).
- For ALB the chart expects an ACM certificate ARN; it is supplied at
  install time via
  `--set 'ui.ingress.annotations.alb\.ingress\.kubernetes\.io/certificate-arn=...'`
  rather than via a Kubernetes Secret.

## Required keys

Source of truth: `packages/config/src/secrets.ts`. Each table row maps to a
field on one of the Zod schemas in that file. Update this document whenever
a schema there gains or loses a field.

### `api` (apps/api)

Sourced from the Zod schemas in `packages/config/src/secrets.ts`.

**Instance (`InstanceSecretsSchema`)**

| Key | Required | Notes |
|---|---|---|
| `INSTANCE_NAME` | yes | Logical name of this DPG instance. |
| `INSTANCE_ENV` | yes | `development` or `production`. |

**API server (`ApiSecretsSchema`)**

| Key | Required | Notes |
|---|---|---|
| `API_DOMAIN` | yes | E.g. `http://api.dpg.local`. Used for instance-URL substitution in served network schemas. |
| `API_PORT` | optional | Defaults to `2742`. |

**Auth (`AuthSecretsSchema`)**

| Key | Required | Notes |
|---|---|---|
| `AUTH_SECRET` | yes | Min 8 chars. In Helm, generated by `install.sh` as `openssl rand -hex 32`. Locally, set in `.env`. |
| `AUTH_MIDDLEWARE_ENABLED` | optional | Defaults to `true`. Set to `false` only for local debugging. |
| `CREATE_TEST_OTP` | optional | Defaults to `false`. When `true`, `generateOtp` returns the fixed `"000000"` instead of a random 6-digit code — useful for local + smoke tests. |

**PII encryption (`PiiCryptoSecretsSchema`)**

| Key | Required | Notes |
|---|---|---|
| `SIGNALS_PII_KEY` | yes | AES-256 master key for encrypting `item_private_state` at rest. Base64-encoded 32 bytes (decoded length must be exactly 32). In Helm, generated by `install.sh` as `openssl rand -base64 32`. Locally, set in `.env`. Loss of this key = loss of all stored PII; rotation requires re-encrypting every row (no in-place rotation tooling yet — the `v1:` envelope prefix is the hook for a future `v2:` rotation path). Treat compromise as a full PII leak. |

**Database (`DatabaseSecretsSchema`)**

| Key | Required | Notes |
|---|---|---|
| `POSTGRES_URL` | optional | Full connection string. If unset, the API composes one from the discrete fields below. |
| `POSTGRES_USER` | yes | |
| `POSTGRES_PASSWORD` | yes | Min 8 chars. |
| `POSTGRES_DB` | yes | |
| `POSTGRES_HOST` | optional | Defaults to `127.0.0.1`. In Helm: `dpg-postgresql`. |
| `POSTGRES_PORT` | optional | Falls back to `DATABASE_PORT` then `5432`. |
| `DATABASE_PORT` | optional | Defaults to `5432`. Used as fallback for `POSTGRES_PORT`. |
| `REDIS_URL` | optional | Full Redis URL. If unset, host/port/password are used directly. |
| `REDIS_HOST` | optional | Defaults to `127.0.0.1`. In Helm: `dpg-redis-master`. |
| `REDIS_PASSWORD` | yes | |
| `REDIS_PORT` | optional | Defaults to `6370` (local). Helm chart sets `6379` via `redis.port`. |

**Network runtime (`NetworkRuntimeSecretsSchema`)**

| Key | Required | Notes |
|---|---|---|
| `SERVED_DOMAINS` | yes | At least one binding required. Comma-separated `network/role` tuples (e.g. `yellow_dot/student,blue_dot/seeker`). Resolved by `packages/config/src/network_runtime.ts`. |
| `NETWORK_CONFIG_SOURCE` | optional | `local` (default) or `remote`. |
| `NETWORK_CONFIG_LOCAL_FILE` | optional | Path(s) resolved relative to `process.cwd()` by `network_config_loader.ts`. Defaults to `../../examples/schemas/yellow_dot/network.json` (the API process runs from `apps/api/`). Helm sets `/app/schemas/yellow_dot.json,/app/schemas/blue_dot.json`. |
| `NETWORK_CONFIG_URLS` | conditional | Required when `NETWORK_CONFIG_SOURCE=remote` and no `SCHEMA_REGISTRY_URL` is set. |
| `ALLOW_EXTRA_SCHEMA_DATA` | optional | Defaults to `false`. |

**Schema registry (`SchemaRegistrySecretsSchema` / `OptionalSchemaRegistrySecretsSchema`)**

| Key | Required | Notes |
|---|---|---|
| `SCHEMA_REGISTRY_URL` | conditional | Required in remote mode unless `NETWORK_CONFIG_URLS` is provided. Optional in local mode. |

**Notification service (`NotificationSecretsSchema`)**

All fields optional; if unset, OTP / SMS-template features are disabled.

| Key | Required | Notes |
|---|---|---|
| `NOTIFICATION_SERVICE_ENDPOINT` | optional | E.g. `http://dpg-notification-service:3000`. |
| `NOTIFICATION_SERVICE_KEY_ID` | optional | Must match a `keyId` in the notification service's `internal-secrets.json`. |
| `NOTIFICATION_SERVICE_SECRET` | optional | HMAC secret paired with `NOTIFICATION_SERVICE_KEY_ID`. `openssl rand -hex 32`. |
| `SMS_TEMPLATE_ID` | optional | |

**Match score / signals-search (`MatchScoreSecretsSchema`)**

All fields optional; if unset (or `SIGNALS_SEARCH_ENDPOINT` / `SIGNALS_SEARCH_API_KEY`
missing), the `match_score` API is not configured and the calculate endpoint
returns `503`.

| Key | Required | Notes |
|---|---|---|
| `MATCH_SCORE_PROVIDER` | optional | Only `signals_search` (the in-network relevance API). |
| `SIGNALS_SEARCH_ENDPOINT` | optional | signals-search base URL, e.g. `http://signals-search:3100`. Required when `MATCH_SCORE_PROVIDER=signals_search`. |
| `SIGNALS_SEARCH_API_KEY` | optional | `x-api-key` valid in the shared Signals `apikey` store. Required when `MATCH_SCORE_PROVIDER=signals_search`. |
| `SIGNALS_SEARCH_RELEVANCE_PATH` | optional | Empty → client uses default `v1/relevance`. |

**Not in the Zod schemas but read elsewhere**

| Key | Required | Notes |
|---|---|---|
| `ALLOWED_ORIGINS` | optional | Read in `packages/config/src/allowed_origins.ts`. Comma-separated origins. In dev, **augments** the defaults; in production (`NODE_ENV=production`), strictly **overrides**. |
| `NODE_ENV` | optional | Standard Node convention; switches several runtime behaviours (e.g. the `ALLOWED_ORIGINS` augment-vs-override rule). |

### `signal-processor` (apps/signal-processor — Plan 3, future)

Reserved for the Plan 3 derived-signals worker. No Zod schema exists in this
repo yet; this section will be filled in when Plan 3 lands. Expected starting
point:

| Key | Required | Notes |
|---|---|---|
| `POSTGRES_URL` | yes | Same connection string as `api`; processor will upsert into derived-signal tables. |
| `REDIS_URL` | optional | If the processor needs the shared session/OTP store. |
| `LOG_LEVEL` | optional | Defaults to `info`. |

### `ui` (apps/ui)

The UI is a Vite-built static bundle. "Secrets" here are baked at **build
time** into the JS bundle, not read at container runtime. Defaults are read
in `apps/ui/src/lib/api-config.ts`.

| Key | Required | Notes |
|---|---|---|
| `VITE_API_URL` | optional | Defaults to `http://localhost:2742`. Used when the UI talks directly to an API (no nginx proxy). |
| `VITE_API_URLS` | optional | JSON-encoded `{ key: url }` map; adds extra endpoints to the instance selector. Invalid JSON is silently ignored. |
| `VITE_DEFAULT_API_URL` | optional | Wins over `VITE_API_URL` when set. |
| `VITE_SHOW_INSTANCE_SELECTOR` | optional | When `true`, surfaces the instance selector even outside `import.meta.env.DEV`. |
| `VITE_NETWORK_ID` | optional | Comma-separated list of network IDs to scope the UI to. Parsed by `parseNetworkIds` (split on `,`, trimmed, empties dropped) in `apps/ui/src/pages/home-page.tsx` and `profile-form-page.tsx`. Empty/unset = no network filter applied. |
| `VITE_FREETEXT_MATCH_SCORE_ENABLED` | optional | Gates the **free-text / no-profile** match score (the `/discover` relevance badge shown to signed-out viewers when they search). Default **ON**; set `false`/`0`/`off`/`no` to hide it. The profile-to-profile match score (signed-in viewer with a profile) is never affected. See `apps/ui/src/lib/match-score-config.ts`. |
| `VITE_MAP_MARKER_CAP_CLUSTERED` | optional | Max map markers fetched/rendered **per domain** while zoomed out (clustering absorbs density). Fetch limit is `cap+1`; the "N+ in this area — zoom in" pill triggers past it. Default **1000**. Runtime-env (retunable per deploy, no rebuild). Lower it (e.g. 300) if the map janks on dense data. See `apps/ui/src/lib/map-caps.ts`. |
| `VITE_MAP_MARKER_CAP_INDIVIDUAL` | optional | Same as above but for zoomed-**in** views (past `VITE_MAP_CLUSTER_DISABLE_ZOOM`). Default **500**; lower it (e.g. 150) for denser data. |
| `VITE_MAP_CLUSTER_DISABLE_ZOOM` | optional | Zoom level at/above which the **individual** cap applies (below it, the **clustered** cap). Default **14**. Note: this selects which cap is used — it does **not** disable Leaflet's own 80px visual clustering. |
| `VITE_MAP_CLUSTER_ZOOM_ANIM_MS` | optional | Duration (ms) of the smooth zoom animation when a Google-map cluster is clicked. Default **2000**; `0` = instant (no animation). Runtime-env (retunable per deploy, no rebuild). See `apps/ui/src/components/map/providers/google-maps-provider.tsx`. |
| `VITE_MAP_FETCH_LIMIT` | optional | **Legacy / radius-only fallback.** Flat marker-fetch cap used **only** when a viewport has *no* bbox (rare radius-only callers); the live map view (bbox path) uses the zoom-band caps above (`cap+1`), **not** this. Default **25000** (also the server's max markers cap). Build-time (`import.meta.env`), unlike the runtime caps above. In practice the live portal/tourist maps always send a bbox, so this doesn't bound the map you see — safe to omit from a deployment. See `apps/ui/src/lib/network-api.ts`. |

In the default Helm install the UI's nginx reverse-proxies `/api/*` to
`dpg-api:2742`, so `VITE_API_URL` is intentionally left empty in
`values.yaml` (the browser talks to `/api/...` on the UI host).

## Rotation

- `AUTH_SECRET`: rotate by deploying a new value; existing sessions are
  invalidated. There is no backward-compatibility window today — this is a
  known sharp edge.
- `POSTGRES_*` / `REDIS_*`: connection-string rotation is the operator's
  responsibility. Update the relevant Secret (or `values.yaml` +
  `helm upgrade`), then trigger a rolling restart of the api pod (and the
  signal-processor pod once Plan 3 ships).
- `NOTIFICATION_SERVICE_SECRET`: rotate the corresponding key entry in
  `internal-secrets.json` **and** the matching env var on the api pod in the
  same deploy, otherwise HMAC verification will fail on one side.
- `SIGNALS_SEARCH_API_KEY`: rotate the key in the shared Signals `apikey`
  store (via better-auth) **and** the env var on the api pod together; a
  mismatch makes signals-search reject match-score requests with `401`.

## Anti-patterns we've already hit

- **`.env.example` carrying broken defaults.** PRs #4–#6 chased values that
  worked for the original vendor but not for us. When you change
  `.env.example`, also change the relevant `process.env.X` default in code
  so a fresh clone without `.env` still boots (e.g.
  `NETWORK_CONFIG_LOCAL_FILE`'s default in
  `packages/config/src/secrets.ts`).
- **Override-without-merge env vars.** `ALLOWED_ORIGINS` originally
  replaced its defaults instead of augmenting them in dev, which silently
  broke `pnpm dev:ui` after a misconfigured `.env`. New pattern: env vars
  that **augment** defaults in dev and **override** in production. Apply
  the same shape to any future origin-list-style config.
- **Secrets in `values.yaml` once `install.sh` has run.** `install.sh`
  populates `credentials.*` in place; do not commit that file after
  running it. The inverse helper in `install.sh` resets the placeholders
  before the next run — use it if you need a clean diff.
